/**
 * Replaced in dist/main by scripts/generate_resource_manifest.ts immediately
 * before packaging. Keeping the trust anchor inside app.asar prevents the
 * detached manifest from choosing its own verification key.
 */
export const RESOURCE_PUBLIC_KEY_SPKI_BASE64 = 'AKAGI_RESOURCE_PUBLIC_KEY_PLACEHOLDER';
